# Codex Guidance

- Do not violate the dependency policy for `backend/src/rop/domain`.
- Always run `make lint`, `make typecheck`, and `make test` after changes.
- Prefer minimal files and small diffs.
- Do not add libraries unless explicitly asked.
